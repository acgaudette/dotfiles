#!/bin/env bash

OUT=~/grabs

if [ "$1" ]
  then name=$1
  else name=$(date +"%F-%H%M%S")
fi

mkdir -p $OUT
if scrot -s "$name.png"; then
  [ `pwd` != $OUT ] && mv "$name.png" $OUT
  echo Saved to $OUT
fi
